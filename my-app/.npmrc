# @visx/* packages declare react 16/17/18 as a peer but work fine with
# React 19; yarn accepts the range mismatch as a warning, npm ci does
# not. Set legacy-peer-deps so CI's `npm ci` matches local `yarn install`.
legacy-peer-deps=true
